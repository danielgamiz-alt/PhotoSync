<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- All items live in the overflow menu (the default for icon-less items),
         so no showAsAction attribute is needed. Order is intentional. -->
    <item
        android:id="@+id/action_sync_now"
        android:title="@string/sync_now" />

    <item
        android:id="@+id/action_settings"
        android:title="@string/menu_settings" />

    <item
        android:id="@+id/action_invite"
        android:title="@string/menu_invite" />

    <item
        android:id="@+id/action_help"
        android:title="@string/menu_help" />
</menu>
